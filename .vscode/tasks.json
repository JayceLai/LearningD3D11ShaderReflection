{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmake:build",
            "type": "shell",
            "command": "cmake --build e:/ShaderLanguage/LearningD3D11ShaderReflection/build --config Debug --target ALL_BUILD -j 10 --"
        }
    ]
}